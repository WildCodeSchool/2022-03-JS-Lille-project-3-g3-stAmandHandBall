import styled from "styled-components";

export default styled.article`
  width: 75vw;
  margin: auto;

  img {
    height: auto;
    width: 10%;
  }
  ul {
    display: flex;
    align-items: center;
    margin: 5%;
  }
  p {
    width: 100%;
    color: var(--white);
  }
`;
